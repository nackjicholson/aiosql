[pytest]
# asyncio: asynchronous tests
markers =
    sqlite3: tests relying on sqlite
    postgres: tests relying on postgres
    mysql: tests relying on mysql
    mariadb: tests relying on mariadb
    duckdb: tests relying on duckdb
    misc: other tests
